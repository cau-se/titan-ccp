apiVersion: v1
kind: Service
metadata:  
  name: cassandra-test
selector:    
  app: cassandra-test
spec:
  type: NodePort
  ports:  
  - name: http
    port: 80
    targetPort: 80
    nodePort: 30036
    protocol: TCP